<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Reunify</title>
    <meta name="description" content="Reunify - Isomorphic JavaScript Apps">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="favicon.png">
    <link rel="apple-touch-icon" href="favicon.png">
    <link rel="manifest" href="manifest.json">
    <meta name="application-name" content="Reunify">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-title" content="Reunify">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <style><%- css %></style>
  </head>
  <body>
    <div id="app">
      <%- html %>
    </div>
    <% if (locals.initialPropsData) { %><script type="application/json" id="initialPropsData"><%- JSON.stringify(locals.initialPropsData) %></script><% } %>
    <script src="bundle.js"></script>
  </body>
</html>
